generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model VideoData {
  title       String?
  description String?
  author      String?
  url         String?
  id          Int     @id @default(autoincrement())
}
